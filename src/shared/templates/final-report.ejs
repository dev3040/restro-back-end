<!DOCTYPE html>
<html dir="ltr" lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <title>Final Analysis Report</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap');
        body {
            font-family: 'Cairo', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            width: 400px;
            margin: 0 auto;
            background: #fff;
            color: #000;
        }
        .header, .footer { text-align: center; }
        .report-box {
            border: 1px solid #333;
            padding: 16px;
            margin-top: 16px;
            background: #fafafa;
        }
        .row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 6px;
            font-size: 14px;
        }
        .label { font-weight: 500; }
        .value { font-family: monospace; }
        .green { color: #1bb81b; font-weight: bold; }
        .divider { border-top: 2px dashed #333; margin: 10px 0; }
        .section-title { font-weight: bold; margin: 10px 0 5px 0; }
    </style>
</head>
<body>
    <div class="header">
        <h2>Final Analysis Report</h2>
        <div class="row">
            <span class="label">From Date :</span>
            <span class="value green"><%= new Date(from).toLocaleDateString('en-GB') %></span>
        </div>
        <div class="row">
            <span class="label">To :</span>
            <span class="value green"><%= new Date(to).toLocaleDateString('en-GB') %></span>
        </div>
        <% if (isHalfDay) { %>
        <div class="row">
            <span class="label">(First Half Only)</span>
        </div>
        <% } %>
    </div>
    <div class="report-box">
        <div class="row"><span class="label">Opening Cash :</span><span class="value">000.000</span></div>
        <div class="row"><span class="label">Taxable Amt :</span><span class="value"><%= taxableAmt?.toFixed(3) || '0.000' %></span></div>
        <div class="row"><span class="label">VAT Amt :</span><span class="value"><%= vatAmt?.toFixed(3) || '0.000' %></span></div>
        <div class="row"><span class="label">Total Sales :</span><span class="value"><%= totalSales?.toFixed(3) || '0.000' %></span></div>
        <div class="row"><span class="label">Home Delivery Sales :</span><span class="value"><%= homeDeliverySales?.toFixed(3) || '0.000' %></span></div>
        <div class="row"><span class="label">Discounts :</span><span class="value"><%= discounts?.toFixed(3) || '0.000' %></span></div>
        <div class="row"><span class="label">Credit Card Sales :</span><span class="value"><%= creditCardSales?.toFixed(3) || '0.000' %></span></div>
        <div class="row"><span class="label">Zomoto Sales :</span><span class="value"><%= zomotoSales?.toFixed(3) || '0.000' %></span></div>
        <div class="row"><span class="label">Talabat Sales :</span><span class="value"><%= talabatSales?.toFixed(3) || '0.000' %></span></div>
        <div class="row"><span class="label">Noon Sales :</span><span class="value"><%= noonSales?.toFixed(3) || '0.000' %></span></div>
        <div class="divider"></div>
        <div class="row section-title">Total After Credi Card & Zomoto & Talabat Sales :</div>
        <div class="row"><span></span><span class="value green"><%= totalAfterOnline?.toFixed(3) || '0.000' %></span></div>
        <div class="divider"></div>
        <div class="row section-title">Total Payments :</div>
        <div class="row"><span></span><span class="value"><%= totalPayments?.toFixed(3) || '0.000' %></span></div>
        <div class="divider"></div>
        <div class="row section-title">Cash In Hand / Balance</div>
        <div class="row"><span></span><span class="value green"><%= cashInHand?.toFixed(3) || '0.000' %></span></div>
        <div class="divider"></div>
        <div class="row section-title">Total Cheque Amt :</div>
        <div class="row"><span></span><span class="value green"><%= totalCheque?.toFixed(2) || '0.00' %></span></div>
    </div>
</body>
</html> 