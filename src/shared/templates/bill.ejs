<!DOCTYPE html>
<html dir="ltr" lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bill <%= bill?.billingId %>
    </title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Roboto+Mono:wght@400;700&display=swap');
        @import url('https://fonts.googleapis.com/css2?family=Noto+Sans+Arabic:wght@400;700&display=swap');

        /* TMT-20 Receipt Printer Optimizations */
        @media print {
            body {
                width: 72mm !important; /* Standard thermal printer width */
                margin: 0 !important;
                padding: 0 !important;
                font-size: 12px !important; /* Increased from 10px */
            }
            
            .header img {
                max-width: 60px !important; /* Increased from 50px */
            }
            
            .items-table {
                font-size: 11px !important; /* Increased from 9px */
            }
            
            .items-table th,
            .items-table td {
                padding: 3px 2px !important; /* Increased padding */
            }
            
            .info, .summary, .footer, .customer-info {
                font-size: 11px !important; /* Increased from 9px */
            }
            
            .totals {
                font-size: 13px !important; /* Increased from 10px */
            }
            
            h2 {
                font-size: 16px !important; /* Increased from 12px */
            }
            
            h3 {
                font-size: 14px !important; /* Increased from 11px */
            }
        }

        body {
            font-family: 'Roboto Mono', 'Courier New', 'Noto Sans Arabic', monospace;
            width: 72mm; /* TMT-20 standard width */
            margin: 0 auto;
            background: #fff;
            color: #000;
            font-size: 12px; /* Increased from 10px */
            line-height: 1.3; /* Increased from 1.2 for better readability */
            -webkit-font-smoothing: none; /* Better for thermal printing */
            -moz-osx-font-smoothing: none;
        }

        .header,
        .footer {
            text-align: center;
            margin-bottom: 8px;
        }

        .header img {
            max-width: 60px; /* Increased from 50px */
            margin-bottom: 4px; /* Increased from 3px */
        }

        .info,
        .summary {
            font-size: 11px; /* Increased from 9px */
            margin-bottom: 8px; /* Increased from 6px */
        }

        .address {
            font-size: 11px; /* Increased from 9px */
            margin-bottom: 8px; /* Increased from 6px */
            word-wrap: break-word;
            white-space: normal;
            line-height: 1.3; /* Increased from 1.2 */
            direction: rtl;
            text-align: right;
            font-family: 'Noto Sans Arabic', 'Arial Unicode MS', 'Tahoma', sans-serif;
            unicode-bidi: bidi-override;
        }

        .items-table {
            width: 100%;
            border-collapse: collapse;
            font-size: 11px; /* Increased from 9px */
            margin-bottom: 10px; /* Increased from 8px */
        }

        .items-table th,
        .items-table td {
            border-bottom: 1px dashed #000;
            padding: 3px 2px; /* Increased from 2px 1px */
            text-align: left;
            vertical-align: top;
        }

        .items-table th {
            border-top: 1px solid #000;
            border-bottom: 1px solid #000;
            background: #f8f8f8;
            font-weight: bold;
        }

        .items-table td:nth-child(1) { /* S.N */
            width: 8%;
        }
        
        .items-table td:nth-child(2) { /* Name */
            width: 40%;
        }
        
        .items-table td:nth-child(3) { /* Rate */
            width: 15%;
            text-align: right;
        }
        
        .items-table td:nth-child(4) { /* Qty */
            width: 12%;
            text-align: center;
        }
        
        .items-table td:nth-child(5) { /* Net */
            width: 25%;
            text-align: right;
        }

        .summary {
            border-top: 1px solid #000;
            padding-top: 8px; /* Increased from 6px */
            font-size: 11px; /* Increased from 9px */
        }

        .summary > div {
            margin-bottom: 3px; /* Increased from 2px */
        }

        .footer {
            margin-top: 15px; /* Increased from 12px */
            font-size: 11px; /* Increased from 9px */
            border-top: 1px dashed #000;
            padding-top: 8px; /* Increased from 6px */
        }

        .totals {
            font-weight: bold;
            font-size: 13px; /* Increased from 10px */
        }

        /* Thermal printer specific optimizations */
        h2, h3 {
            margin: 6px 0; /* Increased from 4px */
            font-size: 16px; /* Increased from 12px */
        }

        h3 {
            font-size: 14px; /* Increased from 11px */
        }

        /* Ensure text doesn't wrap unnecessarily */
        .no-wrap {
            white-space: nowrap;
        }

        /* Customer and remarks section */
        .customer-info {
            margin-top: 10px; /* Increased from 8px */
            font-size: 11px; /* Increased from 9px */
            border-top: 1px dashed #000;
            padding-top: 8px; /* Increased from 6px */
        }
    </style>
</head>

<body>
    <div class="header">
        <% if (bill?.isTakeAway) { %>
            <h3>**** Takeaway ****</h3>
            <% } else if (bill?.isHomeDelivery) { %>
                <h3>**** Home Delivery ****</h3>
                <% } else { %>
                    <h3>**** Dine in ****</h3>
                    <% } %>
                        <h2>TAX INVOICE</h2>
                        <% if (bill?.branch?.logo) { %>
                            <img src="<%= bill?.branch?.logo %>" alt="Logo">
                            <% } %>

                                <div><strong>
                                        <%= bill?.branch?.name || 'RESTAURANT NAME' %>
                                    </strong></div>
                                <div class="info">
                                    <div>TRN: <%= bill?.branch?.prnNum || 'XXXXXXXXXXXXXXX' %>
                                    </div>
                                    <div class="address">
                                        <span lang="ar" dir="rtl"><%= bill?.branch?.address || 'Address not available' %></span>
                                    </div>
                                </div>
                                <div class="info">
                                    Bill No: <%= bill?.billingId %><br>
                                        Date: <%= bill?.createdAt ? new Date(bill.createdAt).toLocaleDateString('en-GB')
                                            : '' %> &nbsp;
                                            Time: <%= bill?.createdAt ? new
                                                Date(bill.createdAt).toLocaleTimeString('en-US', { hour: 'numeric' ,
                                                minute: 'numeric' , hour12: true, timeZone: 'Asia/Dubai' }) : '' %>
                                </div>
    </div>
    <table class="items-table">
        <thead>
            <tr>
                <th>S.N</th>
                <th>Name</th>
                <th>Rate</th>
                <th>Qty</th>
                <th>Net</th>
            </tr>
        </thead>
        <tbody>
            <% bill?.billingCalc?.items?.forEach((item, idx)=> { %>
                <tr>
                    <td>
                        <%= idx + 1 %>
                    </td>
                    <td>
                        <%= item?.name %>
                    </td>
                    <td class="no-wrap">
                        <%= item?.price?.toFixed(2) %>
                    </td>
                    <td>
                        <%= item?.quantity %>
                    </td>
                    <td class="no-wrap">
                        <%= (item?.price * item?.quantity)?.toFixed(2) %>
                    </td>
                </tr>
                <% }) %>
        </tbody>
    </table>
    <div class="summary">
        <div>Total Items: <%= bill?.billingCalc?.items?.length || 0 %>
        </div>
        <div>Total Before VAT: <%= bill?.billingCalc?.amountWithOutVat?.toFixed(2) || '0.00' %>
        </div>
        <div>Incl. VAT 5%: <%= bill?.billingCalc?.vat?.toFixed(2) || '0.00' %>
        </div>
        <div class="totals">Total Amt (AED): <%= bill?.subTotal || '0.00' %>
        </div>
        <div>Payment Method: <%= bill?.paymentMethod?.name || '' %>
        </div>
    </div>
    <div class="footer">
        <div>Thank You, Visit Again</div>
    </div>
    <% if (bill?.customer?.name || bill?.remarks) { %>
        <div class="customer-info" style="margin-bottom:20px">
            <% if (bill?.customer?.name) { %>
                <div>
                    Customer: <%= bill?.customer?.name %>
                    <% if (bill?.customer?.phone) { %>
                        (<%= bill?.customer?.phone %>)
                    <% } %>
                </div>
            <% } %>
            <% if (bill?.remarks) { %>
                <div>Remarks: <%= bill?.remarks %></div>
            <% } %>
            <% if (bill?.customer?.address && bill?.isHomeDelivery === true) { %>
                <div>Address: <%= bill?.customer?.address %></div>
            <% } %>
        </div>
    <% } %>
</body>

</html>
